<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/colorMenu"
                android:centerColor="@color/colorMenu"
                android:centerY="1.0"
                android:endColor="@color/colorMenu"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/colorOptionMenuHistory"
                    android:centerColor="@color/colorOptionMenuHistory"
                    android:centerY="1.0"
                    android:endColor="@color/colorOptionMenuHistory"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>